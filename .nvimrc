so ~/.dotfiles/.vimrc

" Neovim stuff
" ==============

" Window split settings
highlight TermCursor ctermfg=red guifg=red
set splitbelow
set splitright

" Terminal settings
tnoremap <ESC><ESC> <C-\><C-n>
